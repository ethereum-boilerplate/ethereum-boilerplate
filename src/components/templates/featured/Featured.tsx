import { Heading, VStack } from '@chakra-ui/react';

const Featured = () => {
  return (
    <VStack w={'full'}>
      <Heading size="md" marginBottom={6}>
        Featured NFTs
      </Heading>
    </VStack>
  );
};

export default Featured;
