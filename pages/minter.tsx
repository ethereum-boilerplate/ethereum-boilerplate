import { Default } from 'components/layouts/Default';
import { Minter } from 'components/templates/minter';

const MinterPage = () => {
  return (
    <Default pageName="Minter">
      <Minter />
    </Default>
  );
};

export default MinterPage;
